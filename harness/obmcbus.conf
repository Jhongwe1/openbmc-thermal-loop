<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-Bus Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<!--
  W7 L2 的私有 D-Bus(harness/l2_ab.sh 用)。

  為什麼不用系統匯流排:swampd 要 own 名字、mock mapper 要冒充
  xyz.openbmc_project.ObjectMapper —— 在系統匯流排上這兩件事都需要
  policy 檔與 root;私有匯流排上全放行,乾淨、可重複、砍掉即忘。

  地雷 #14:預設 policy 會拒絕 own 名字(refused by policy),
  所以這裡把 own/send/receive 全部明文放行。
-->
<busconfig>
  <type>system</type>
  <listen>unix:path=/tmp/obmcbus/socket</listen>
  <policy context="default">
    <allow user="*"/>
    <allow own="*"/>
    <allow send_type="method_call"/>
    <allow send_type="signal"/>
    <allow send_type="method_return"/>
    <allow send_type="error"/>
    <allow receive_type="method_call"/>
    <allow receive_type="signal"/>
    <allow receive_type="method_return"/>
    <allow receive_type="error"/>
  </policy>
</busconfig>
