option(
    'tests',
    type: 'feature',
    value: 'auto',
    description: '建置 L0 gtest（auto = 有 gtest 就建）',
)

# W5 之後會加：
# option('with-upstream-parity', type: 'boolean', value: true,
#        description: 'Build the gtest that compares against upstream ec::pid()')
